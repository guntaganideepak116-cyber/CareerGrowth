# ==========================================
# FRONTEND CONFIGURATION (For Vercel)
# ==========================================

# The URL where your backend is deployed (e.g. https://your-backend.onrender.com)
# For local development, use http://localhost:5000
VITE_API_URL="https://your-backend-app.onrender.com"

# Firebase Client SDK Configuration (Get from Firebase Console)
VITE_FIREBASE_API_KEY="your-api-key"
VITE_FIREBASE_AUTH_DOMAIN="your-project.firebaseapp.com"
VITE_FIREBASE_PROJECT_ID="your-project-id"
VITE_FIREBASE_STORAGE_BUCKET="your-project.appspot.com"
VITE_FIREBASE_MESSAGING_SENDER_ID="your-sender-id"
VITE_FIREBASE_APP_ID="your-app-id"

# ==========================================
# BACKEND CONFIGURATION (For Render/Railway/Heroku)
# ==========================================

# Port (Cloud providers usually set this automatically, but set to 5000 as fallback)
PORT=5000

# Admin Email for Dashboard Access
ADMIN_EMAIL="admin@example.com"

# AI Service Keys (You only need one active, or both if switching)
OPENAI_API_KEY="sk-..."
GEMINI_API_KEY="AIza..."

# Firebase Admin SDK credentials
# OPTION 1: Use a one-line JSON string (Recommended for cloud deployment)
# Get the content of serviceAccountKey.json, remove newlines, & wrap in single quotes if needed
FIREBASE_SERVICE_ACCOUNT_JSON='{"type": "service_account", "project_id": "...", ...}'

# OPTION 2: File path (Only if you can upload secure files, e.g. Render Secret Files)
# GOOGLE_APPLICATION_CREDENTIALS=/etc/secrets/serviceAccountKey.json
