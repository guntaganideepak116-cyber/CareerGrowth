---
description: Automatically sync changes to GitHub and Vercel
---
After completing any code changes requested by the user, follow these steps to ensure GitHub and Vercel are updated:

1. Check for changes
// turbo
2. git add .
// turbo
3. git commit -m "update: [brief description of changes made]"
// turbo
4. git push origin main

Note: This triggers a Vercel build automatically.
